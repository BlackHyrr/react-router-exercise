import React from 'react';
import Input from '../../components/Input';

const AddPost = () => {
    return (
        <div>
            <h1>Add a Post</h1>
            <Input />
        </div>
    );
};

export default AddPost;